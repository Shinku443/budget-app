CREATE TABLE pending_mutations (
    id TEXT PRIMARY KEY,
    entity_type TEXT NOT NULL, -- "transaction" or "category"
    operation TEXT NOT NULL,   -- "create", "update", "delete"
    payload TEXT NOT NULL      -- JSON payload
);
